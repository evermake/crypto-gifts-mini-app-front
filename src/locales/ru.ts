import type { Locale } from '.'

export default ((): Locale => ({
  tapbar: {
    tab: {
      store: 'Магазин',
      gifts: 'Подарки',
      leaderboard: 'Топ',
      profile: 'Профиль',
    },
  },
}))()
